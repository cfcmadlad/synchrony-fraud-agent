# Safe to expose to the browser (anon key is designed for this; RLS policies do the real
# access control). Never put the service_role key here.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=replace-me
VITE_API_BASE_URL=http://localhost:8000
