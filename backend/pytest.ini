[pytest]
testpaths = tests
pythonpath = .
addopts = -m "not integration"
markers =
    integration: hits the live Supabase project and trained model artifacts, and permanently inserts a real transaction row (run explicitly with `pytest -m integration`)
